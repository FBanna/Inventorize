CREATE TABLE IF NOT EXISTS components
(
    NAME    TEXT                    NOT NULL,
    SIZE    TEXT,
    INFO    TEXT,
    STOCK   INTEGER                 NOT NULL,
    ORIGIN  TEXT,
    URL     TEXT
);


CREATE TABLE IF NOT EXISTS components
(
    /*id          INTEGER PRIMARY KEY NOT NULL,*/
    name        TEXT        NOT NULL,
    size        TEXT        NOT NULL,
    value       TEXT        NOT NULL,
    count       INTEGER     NOT NULL,

) STRICT;
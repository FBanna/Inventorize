{
    "type": "array",

    "items": {
        "type": "object",
        "required": [ "name", "object_type", "unit" ],
        "properties": {

            "name": { "type": "string" },
            "object_type": { "enum": ["string", "integer", "float", "boolean", "date"] },
            "unit": { "type": "string" }

        }
    }
}